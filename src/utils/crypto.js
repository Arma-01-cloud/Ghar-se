// Note: Client-side password storage has been deprecated and migrated to Supabase Auth.
// Passwords are now securely handled by Supabase Auth (auth.users) using server-side bcrypt.
export const CLIENT_PASSWORD_STORAGE_DEPRECATED = true;